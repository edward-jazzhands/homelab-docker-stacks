services:
  neo4j:
    image: neo4j:5-community
    container_name: kg_neo4j
    environment:
      NEO4J_AUTH: neo4j/kg_password
    ports:
      - "7474:7474"   # Browser UI
      - "7687:7687"   # Bolt protocol (used by Python driver)
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data: